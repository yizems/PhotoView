def url= System.getenv("MAVEN_PATH")
println(url)
ext {
    MAVEN_URL = "${url}repository/android-repositories/"
    MAVEN_UPLOAD_URL = "${url}repository/android-repositories/"
    MAVEN_USER = "dev"
    MAVEN_PWD = "1234.com"
}